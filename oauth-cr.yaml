apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - htpasswd:
      fileData:
        name: nclab-user
    mappingMethod: claim
    name: lab-user
    type: HTPasswd
